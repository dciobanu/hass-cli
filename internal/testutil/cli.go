package testutil

// CLI test execution helpers are not needed for Phase 1-4 since we test
// the API and WebSocket client layers directly. CLI integration tests
// (Phase 5) would add helpers here for running Cobra commands in-process.
